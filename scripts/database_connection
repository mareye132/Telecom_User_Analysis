# db_config.py

import psycopg2

def get_db_connection():
    """Create and return a PostgreSQL database connection."""
    conn = psycopg2.connect(
        dbname='postgres',
        user='postgres',
        password='Maru@132',
        host='localhost',
        port='5432'
    )
    return conn
